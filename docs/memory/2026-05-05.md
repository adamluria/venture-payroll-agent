# Session — 2026-05-05

**Contributor:** Adam Luria + Claude (Cowork)

## CURRENT STATE

Active feature: none — Session 1 (Phase 1 setup) complete
Last completed: First Cloud Run deploy; URL recorded in auto-memory
In progress: nothing
Blocked on: nothing — ready for Session 2 (build core prototype with mock data) or Session 3 (Salesforce Connected App)

## Summary

First Cowork session for venture-payroll-agent. Completed the entire Phase 1 setup track in one session:
- Verified scaffold state, corrected stale local-path references in 8 docs (project lives at `~/Downloads/venture-payroll-agent`, not the originally documented `~/Documents/Claude/projects/...`)
- Created `.env.local` from `.env.example` (placeholders only — API keys still TBD)
- Initialized git, made initial scaffold commit (`b8a8629`)
- Created GitHub repo at https://github.com/adamluria/venture-payroll-agent and pushed
- Created GCP project `venture-payroll-agent`, linked billing, enabled Cloud Run / Artifact Registry / Cloud Build APIs
- Created Artifact Registry repo `venture-payroll-agent` in `us-east1`
- Generated `package-lock.json` via `npm install`, committed and pushed
- First Cloud Run deploy succeeded: https://venture-payroll-agent-343168101156.us-east1.run.app

## Decisions Made

- **Local project path: `~/Downloads/venture-payroll-agent`** [Tier 1] — chosen because that's where the user extracted the zip. All 8 doc references updated accordingly.
- **Dedicated GCP project for this app** (not deploying into the existing `solar-journey-xdemandiq` project) — cleanest for cost tracking and IAM.
- **Region: us-east1** [Tier 1] — matches the docs.
- **Docker not installed locally** — fine for now; `gcloud run deploy --source` builds via Cloud Build server-side. If we want local Docker testing later, install Docker Desktop.

## Bugs Fixed

- **First deploy failed: `npm ci` error in Cloud Build** — root cause: no `package-lock.json` existed (we had never run `npm install` locally). Fix: ran `npm install` to generate the lockfile, committed and pushed, redeployed successfully.

## TODO Updates

Phase 1 complete:
- [x] Extract scaffold zip
- [x] `.env.local` from `.env.example` (via `bash setup.sh`)
- [x] `npm install` (generated `package-lock.json`)
- [x] Initial git commit (`b8a8629`)
- [x] Create GitHub repo + push: https://github.com/adamluria/venture-payroll-agent
- [x] GCP project create + billing link
- [x] Enable Cloud Run / Artifact Registry / Cloud Build APIs
- [x] Create Artifact Registry repo in `us-east1`
- [x] First Cloud Run deploy: https://venture-payroll-agent-343168101156.us-east1.run.app
- [x] `.auto-memory/reference_venture-payroll-agent.md` updated with GitHub URL + Cloud Run URL [Tier 1]

Not yet done (intentionally deferred to later sessions):
- [ ] `npm run dev` verification (deploy succeeded so we know the build works; local dev server not yet confirmed)
- [ ] API keys for CoAdvantage, Five9, Salesforce, Google Chat, Google Workspace, Forge — done per-integration in Sessions 3–8
- [ ] Salesforce Connected App + OAuth (Session 3)
- [ ] Branch protection rules on GitHub `main` (mentioned in quickstart, optional for solo work)

## Open Questions

- API keys for the integrations — collected per-integration in their dedicated sessions, no blocker right now.
- Local dev server has not been verified end-to-end. The Cloud Run deploy works (which means `npm run build` works), so dev server is very likely fine, but not formally verified. Worth running `npm run dev` and opening http://localhost:3000 (note: port 3000, not 5173 — `vite.config.js` overrides default) before next session.
